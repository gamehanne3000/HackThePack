import {StyleSheet} from 'react-native';
import {Spacing, Colors} from '@styles/base';

/*
    This is a global stylesheet class that handles all styling
    concerning the layouts of the storage units.
*/

export const CategoryItem = StyleSheet.create({});

export const ListItem = StyleSheet.create({});
